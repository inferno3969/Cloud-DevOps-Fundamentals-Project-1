@page "/add-manager"

@attribute [Authorize(Roles="Administrator")]

<PageTitle>Add Manager</PageTitle>
    <div class="col-md-12">
        <RadzenAlert Shade="Shade.Lighter" Variant="Variant.Flat" Size="AlertSize.Small" AlertStyle="AlertStyle.Danger" Visible="@errorVisible">Cannot save Manager</RadzenAlert>
        <RadzenTemplateForm TItem="CloudDevOpsProject1.Server.Models.DevOps_Proj_Database.Manager" Data="@manager" Visible="@(manager != null && canEdit)" Submit="@FormSubmit">
            <div style="margin-bottom: 1rem" class="row">
                <div class="col-md-3">
                    <RadzenLabel Text="Employee" Component="Employee" style="width: 100%" />
                </div>
                <div class="col-md-9">
                    <RadzenDropDown Data="@employeesForEmpID" TextProperty="Emp_Name" ValueProperty="Emp_ID"
                         Placeholder="Choose Employee" style="display: block; width: 100%" @bind-Value="@manager.Emp_ID" Name="Emp_ID"
                         Count=@employeesForEmpIDCount LoadData=@employeesForEmpIDLoadData />
                </div>
            </div>
            <div style="margin-bottom: 1rem" class="row">
                <div class="col-md-3">
                    <RadzenLabel Text="Plant" Component="Plant" style="width: 100%" />
                </div>
                <div class="col-md-9">
                    <RadzenDropDown Data="@plantsForPlantID" TextProperty="Plant_Name" ValueProperty="Plant_ID"
                         Placeholder="Choose Plant" style="display: block; width: 100%" @bind-Value="@manager.Plant_ID" Name="Plant_ID"
                         Count=@plantsForPlantIDCount LoadData=@plantsForPlantIDLoadData />
                </div>
            </div>
            <div class="row">
                <div class="col d-flex justify-content-end" style="margin-top: 1rem">
                    <RadzenButton ButtonStyle="ButtonStyle.Primary" ButtonType="ButtonType.Submit" Icon="save" Text="Save" Style="margin-right: 0.5rem" Variant="Variant.Flat" />
                    <RadzenButton ButtonStyle="ButtonStyle.Light" Text="Cancel" Variant="Variant.Flat" Click="@CancelButtonClick"  />
                </div>
            </div>
        </RadzenTemplateForm>
    </div>

